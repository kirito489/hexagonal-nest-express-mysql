# =============================================================================
# 容器內的 apps/api/.env——隊友共用的容器基準
# -----------------------------------------------------------------------------
# compose.yml 的 x-app-base 把本檔掛到容器的 apps/api/.env，遮蔽掉 host 的同名檔。
#
# 為什麼要遮：bind mount 會把 host 的 .env 一併帶進容器，而 dotenv 不覆寫既有的
# process.env——也就是「compose 有設的贏、compose 沒設的由 host .env 補」。
# 這讓容器行為取決於開發者本機的設定，其中連線類的會直接打壞它
# （REDIS_URL 指向 host 的 localhost:6379，容器裡什麼都沒有）。
#
# 設定的優先序（高到低）：
#
#   compose 的 environment  >  apps/api/.env.container  >  本檔  >  envSchema 預設
#
# 三者的職責分工：
#
#   compose 的 environment      連線類變數，**釘死**不可被覆寫（有守則盯著）
#   apps/api/.env.container     個人偏好，不進版控，只影響自己這台
#   本檔（進版控）              隊友共用的容器基準，所有人跑起來一樣
#
# 也就是說：**要全隊生效的容器設定寫這裡，只想自己生效的寫 apps/api/.env.container。**
# ⚠️ 注意優先序——本檔的值會被 .env.container 蓋掉，需要不可被覆寫的請寫進
# compose 的 environment。
#
# ⚠️ **為什麼個人覆寫是獨立的 .env.container，不是直接讀 apps/api/.env**：
# compose 的 env 解析器比 dotenv 嚴格，而 `env_file` 解析失敗會讓**所有**
# docker compose 指令失效——連 `config` / `ps` / `down` 都跑不了。
# 實測踩到：`.env` 有一行帶角括號的寄件者位址，對應用程式完全合法，
# 卻讓整個 compose 無法使用。獨立檔就沒有這個耦合。
#
# 改完 `pnpm docker:up` 重啟即可（本檔是 bind mount，不必重建映像）。
# =============================================================================
