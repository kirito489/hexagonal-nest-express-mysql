# =============================================================================
# 容器內的 apps/api/.env
# -----------------------------------------------------------------------------
# compose.yml 的 x-app-base 把本檔掛到容器的 apps/api/.env，**遮蔽掉 host 的同名檔**。
#
# 為什麼要遮：bind mount 會把 host 的 .env 一併帶進容器，而 dotenv 不覆寫既有的
# process.env——也就是「compose 有設的贏、compose 沒設的由 host .env 補」。
# 這讓容器行為取決於開發者本機的 78 個變數，其中連線類的會直接打壞它
# （REDIS_URL 指向 host 的 localhost:6379，容器裡什麼都沒有）。
#
# 遮掉之後，容器的設定只有兩個來源：compose 的 environment，以及 envSchema 的預設值。
#
# 需要容器專屬的覆寫就寫在這裡；改完 `pnpm docker:up` 重啟即可（本檔是 bind mount，
# 不必重建映像）。
# =============================================================================
