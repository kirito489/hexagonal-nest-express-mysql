# =============================================================================
# 建置 context 排除清單
# -----------------------------------------------------------------------------
# node_modules 一定要排除：host 上是 macOS/arm64 的產物（含 bcrypt 等原生模組與
# Prisma 引擎），複製進 Linux 容器會直接壞掉，而且會讓 context 肥好幾百 MB。
# =============================================================================

**/node_modules
**/dist
**/coverage
**/.turbo

# 環境變數不進映像：dev 由 compose 的 environment 提供，正式環境由部署平台注入
**/.env
**/.env.*
!**/.env.example

.git
.gitignore
**/*.log
**/.DS_Store

# 開發／文件用，不影響執行
openspec
tasks
pr
.claude
.agents
**/*.md
!README.md
